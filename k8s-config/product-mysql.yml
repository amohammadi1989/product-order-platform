apiVersion: v1
kind: Service
metadata:
  name: product-mysql
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: product-mysql
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: product-mysql
spec:
  serviceName: product-mysql
  replicas: 1
  selector:
    matchLabels:
      app: product-mysql
  template:
    metadata:
      labels:
        app: product-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: product_db

            - name: MYSQL_USER
              value: product
            - name: MYSQL_PASSWORD
              value: product
          volumeMounts:
            - name: product-mysql-data
              mountPath: /var/lib/product-mysql
  volumeClaimTemplates:
    - metadata:
        name: product-mysql-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi