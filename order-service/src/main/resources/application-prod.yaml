server:
  port: 7069
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/order_db
    username: order
    password: order
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true